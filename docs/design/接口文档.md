# 接口文档

本文档描述 Genban 服务端提供的 HTTP 接口。

---

## 用户模块

### GET /api/user/key

获取 RSA 公钥，用于前端加密密码。

#### 响应参数

| 字段       | 类型   | 说明                     |
| ---------- | ------ | ------------------------ |
| public_key | string | RSA 公钥内容（PEM 格式） |
| error      | string | 错误信息，成功时为空     |

#### 响应示例（成功）

```json
{
  "public_key": "-----BEGIN PUBLIC KEY-----\nMIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA...\n-----END PUBLIC KEY-----",
  "error": ""
}
```

#### 响应示例（失败）

```json
{
  "public_key": "",
  "error": "公钥文件不存在"
}
```

#### 调用时机

前端应在用户输入密码前调用此接口获取公钥，用于加密密码后再调用 `/api/user/login`。

---

### POST /api/user/login

用户登录/自动注册接口。若用户不存在则自动注册，存在则验证登录。

#### 请求参数

| 字段     | 类型   | 必填 | 说明                                  |
| -------- | ------ | ---- | ------------------------------------- |
| user_id  | string | 是   | 用户 ID                               |
| password | string | 是   | 密码（RSA 加密后的密文，Base64 编码） |

#### 请求示例

```json
{
  "user_id": "user001",
  "password": "RSA加密后的密文..."
}
```

#### 密码加密说明

1. 调用 `/api/user/key` 获取 RSA 公钥
2. 前端使用 RSA 公钥对密码进行加密
3. 加密后的二进制数据使用 Base64 编码
4. 将 Base64 字符串作为 password 字段的值

#### 密码强度要求

- 最少 6 位字符
- 注册时若密码强度不足会返回错误

#### 响应参数

| 字段       | 类型    | 说明                     |
| ---------- | ------- | ------------------------ |
| token      | string  | 登录令牌，后续请求需携带 |
| expires_at | integer | 令牌过期时间戳（秒）     |
| error      | string  | 错误信息，成功时为空     |

#### 响应示例（成功）

```json
{
  "token": "x9aBcDeFgHiJkLmNoPqRsTuVwXyZ...",
  "expires_at": 1712345678,
  "error": ""
}
```

#### 响应示例（失败）

```json
{
  "token": "",
  "expires_at": 0,
  "error": "密码最少需要6位字符"
}
```

#### 错误码说明

| 错误信息              | 说明                 |
| --------------------- | -------------------- |
| 密码最少需要 6 位字符 | 注册时密码强度不足   |
| 密码错误              | 登录时密码验证失败   |
| RSA 私钥未配置        | 服务端未配置私钥文件 |
| 用户创建失败          | 数据库写入失败       |
| 用户状态初始化失败    | 用户状态创建失败     |
| 用户名只能包含字母、数字、下划线 | 注册时用户名格式不合法 |

---

## Assistant 模块

### POST /api/assistant/submit

提交用户消息到处理队列。消息将被放入异步队列，由 AgentWorker 在独立线程中处理。

#### 请求头

| 字段          | 必填 | 说明                               |
| ------------- | ---- | ---------------------------------- |
| Authorization | 是   | Bearer <token>，登录接口获取的令牌 |

#### 请求参数

| 字段    | 类型   | 必填 | 说明               |
| ------- | ------ | ---- | ------------------ |
| message | string | 是   | 用户输入的消息内容 |

#### 请求示例

```json
{
  "message": "今天天气怎么样？"
}
```

#### 响应参数

| 字段    | 类型    | 说明                               |
| ------- | ------- | ---------------------------------- |
| success | boolean | 是否成功放入队列                   |
| chat_id | string  | 本次输入新增的 Chat ID，失败时为空 |
| error   | string  | 错误信息，成功时为空               |

#### 响应示例（成功）

```json
{
  "success": true,
  "chat_id": "171234567800010001",
  "error": ""
}
```

#### 响应示例（失败-未鉴权）

```json
{
  "success": false,
  "chat_id": "",
  "error": "未授权，请重新登录"
}
```

---

### GET /api/assistant/stream

建立 SSE 连接，持续接收 Agent 产生的新消息。

#### 请求头

| 字段          | 必填 | 说明                               |
| ------------- | ---- | ---------------------------------- |
| Authorization | 是   | Bearer <token>，登录接口获取的令牌 |

#### SSE 事件格式

| 字段              | 类型    | 说明                                    |
| ----------------- | ------- | --------------------------------------- |
| id                | string  | 消息唯一 ID                             |
| type              | string  | 消息类型：user/assistant/tool/error     |
| time              | integer | 消息时间戳（秒）                        |
| content           | string  | 消息内容                                |
| reasoning_content | string  | 推理内容（仅 assistant 类型可能有）     |
| tool_calls        | array   | 工具调用信息（仅 assistant 类型可能有） |

#### SSE 数据示例

```
event: message
data: {"type": "user", "time": 1712345678, "id": "msg_001", "content": "查询北京天气", "reasoning_content": "", "tool_calls": null}

event: message
data: {"type": "assistant", "time": 1712345678, "id": "msg_002", "content": "", "reasoning_content": "用户想查询天气", "tool_calls": null}

event: message
data: {"type": "assistant", "time": 1712345678, "id": "msg_002", "content": "我来为您查询天气", "reasoning_content": "用户想查询天气，我需要调用天气工具", "tool_calls": [{"function": {"name": "get_weather", "arguments": "{\"location\": \"北京\"}"}}]}

event: message
data: {"type": "tool", "time": 1712345678, "id": "msg_003", "content": "北京今天晴，25°C", "reasoning_content": "", "tool_calls": null}

event: message
data: {"type": "assistant", "time": 1712345678, "id": "msg_004", "content": "", "reasoning_content": "工具返回了天气信息", "tool_calls": null}

event: message
data: {"type": "assistant", "time": 1712345678, "id": "msg_004", "content": "北京今天天气晴朗，气温 25°C", "reasoning_content": "工具返回了天气信息", "tool_calls": null}

```

#### 注意事项

1. **连接时机**：建议在登录成功后立即建立 SSE 连接，确保能接收到所有消息
2. **连接保持**：SSE 连接会一直保持，直到客户端主动断开或网络异常
3. **消息顺序**：消息按时间顺序推送，id 字段可用于排序
4. **错误处理**：网络异常时会触发 onerror，前端应实现重连机制

---

## FileSystem 模块

文件系统模块提供文件操作相关的 HTTP 接口，所有操作限制在用户专属目录下。

### GET /api/file_system/list

查看目录内容。

#### 请求头

| 字段          | 必填 | 说明                               |
| ------------- | ---- | ---------------------------------- |
| Authorization | 是   | Bearer <token>，登录接口获取的令牌 |

#### 请求参数

| 字段 | 类型   | 必填 | 说明                           |
| ---- | ------ | ---- | ------------------------------ |
| path | string | 否   | 相对路径，默认为用户根目录     |

#### 响应参数

| 字段  | 类型  | 说明                 |
| ----- | ----- | -------------------- |
| items | array | 目录项列表           |

items 中每个对象的字段：

| 字段        | 类型    | 说明                           |
| ----------- | ------- | ------------------------------ |
| name        | string  | 名称                           |
| type        | string  | 类型：directory/file           |
| size        | integer | 文件大小（字节），目录为 null  |
| modified_at | string  | 修改时间（ISO 8601 格式）      |

#### 响应示例（成功）

```json
{
  "items": [
    {
      "name": "documents",
      "type": "directory",
      "size": null,
      "modified_at": "2026-03-08T10:30:00"
    },
    {
      "name": "notes.txt",
      "type": "file",
      "size": 1024,
      "modified_at": "2026-03-08T11:00:00"
    }
  ]
}
```

#### 错误码说明

| HTTP 状态码 | 说明         |
| ----------- | ------------ |
| 403         | 无权访问该路径 |
| 404         | 目录不存在   |

---

### GET /api/file_system/preview

预览纯文本文件内容。

#### 请求头

| 字段          | 必填 | 说明                               |
| ------------- | ---- | ---------------------------------- |
| Authorization | 是   | Bearer <token>，登录接口获取的令牌 |

#### 请求参数

| 字段 | 类型   | 必填 | 说明         |
| ---- | ------ | ---- | ------------ |
| path | string | 是   | 文件相对路径 |

#### 响应示例（成功）

纯文本内容：
```
这是文件的内容...
```

#### 错误码说明

| HTTP 状态码 | 说明             |
| ----------- | ---------------- |
| 403         | 无权访问该路径   |
| 404         | 文件不存在       |
| 400         | 路径不是文件或文件不是纯文本格式 |

---

### GET /api/file_system/download

下载文件。

#### 请求头

| 字段          | 必填 | 说明                               |
| ------------- | ---- | ---------------------------------- |
| Authorization | 是   | Bearer <token>，登录接口获取的令牌 |

#### 请求参数

| 字段 | 类型   | 必填 | 说明         |
| ---- | ------ | ---- | ------------ |
| path | string | 是   | 文件相对路径 |

#### 响应示例（成功）

文件下载流（Content-Disposition: attachment）。

#### 错误码说明

| HTTP 状态码 | 说明           |
| ----------- | -------------- |
| 403         | 无权访问该路径 |
| 404         | 文件不存在     |
| 400         | 路径不是文件   |

---

### POST /api/file_system/upload

上传文件。

#### 请求头

| 字段          | 必填 | 说明                               |
| ------------- | ---- | ---------------------------------- |
| Authorization | 是   | Bearer <token>，登录接口获取的令牌 |
| Content-Type  | 是   | multipart/form-data                |

#### 请求参数

| 字段 | 类型   | 必填 | 说明                       |
| ---- | ------ | ---- | -------------------------- |
| path | string | 是   | 目标相对路径（含文件名）   |
| file | file   | 是   | 上传的文件                 |

#### 响应参数

| 字段   | 类型    | 说明           |
| ------ | ------- | -------------- |
| success| boolean | 是否成功       |
| path   | string  | 保存路径       |
| size   | integer | 文件大小（字节）|

#### 响应示例（成功）

```json
{
  "success": true,
  "path": "documents/report.pdf",
  "size": 2048
}
```

#### 错误码说明

| HTTP 状态码 | 说明           |
| ----------- | -------------- |
| 403         | 无权访问该路径 |

---

## Skills 模块

Skills 模块提供 Skill 管理相关的 HTTP 接口，所有操作限制在用户专属的 skills 目录下。

### GET /api/skills/list

查询当前用户所有 Skills。

#### 请求头

| 字段          | 必填 | 说明                               |
| ------------- | ---- | ---------------------------------- |
| Authorization | 是   | Bearer <token>，登录接口获取的令牌 |

#### 响应参数

| 字段   | 类型  | 说明           |
| ------ | ----- | -------------- |
| skills | array | Skill 列表     |

skills 中每个对象的字段：

| 字段        | 类型   | 说明           |
| ----------- | ------ | -------------- |
| id          | string | Skill ID（文件夹名） |
| name        | string | Skill 名称     |
| description | string | Skill 描述内容 |

#### 响应示例（成功）

```json
{
  "skills": [
    {
      "id": "example-skill",
      "name": "示例 Skill",
      "description": "示例 Skill 描述"
    }
  ]
}
```

#### 响应示例（空列表）

```json
{
  "skills": []
}
```

---

### GET /api/skills/content

获取指定 Skill 的 SKILL.md 文件内容。

#### 请求头

| 字段          | 必填 | 说明                               |
| ------------- | ---- | ---------------------------------- |
| Authorization | 是   | Bearer <token>，登录接口获取的令牌 |

#### 请求参数

| 字段    | 类型   | 必填 | 说明           |
| ------- | ------ | ---- | -------------- |
| skill_id | string | 是   | Skill ID（文件夹名） |

#### 响应示例（成功）

纯文本内容（SKILL.md 文件内容）：
```markdown
---
name: example-skill
description: 示例 Skill 描述
---

# Skill 说明

这是 Skill 的详细说明...
```

#### 错误码说明

| HTTP 状态码 | 说明                          |
| ----------- | ----------------------------- |
| 404         | Skill 不存在或 SKILL.md 不存在 |

---

### DELETE /api/skills/delete

删除指定 Skill。

#### 请求头

| 字段          | 必填 | 说明                               |
| ------------- | ---- | ---------------------------------- |
| Authorization | 是   | Bearer <token>，登录接口获取的令牌 |

#### 请求参数

| 字段    | 类型   | 必填 | 说明           |
| ------- | ------ | ---- | -------------- |
| skill_id | string | 是   | Skill ID（文件夹名） |

#### 响应参数

| 字段    | 类型    | 说明           |
| ------- | ------- | -------------- |
| success | boolean | 是否删除成功   |

#### 响应示例（成功）

```json
{
  "success": true
}
```

#### 错误码说明

| HTTP 状态码 | 说明           |
| ----------- | -------------- |
| 404         | Skill 不存在   |

---

## 通用说明

### 鉴权方式

所有需要鉴权的接口（除 `/api/user/key` 和 `/api/user/login` 外）都需要在请求头中携带 Token：

```
Authorization: Bearer <token>
```

Token 通过 `/api/user/login` 接口获取，有效期为 7 天。

### 错误响应格式

当接口返回 401 状态码时，响应体格式如下：

```json
{
  "detail": "未授权，请重新登录"
}
```

前端应在收到 401 时引导用户重新登录。
